{% extends 'base.html.twig' %}

{% block title %}Modifier mon profil{% endblock %}

{% block body %}
<style>
    .edit-section {
        background: rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(10px);
        -webkit-backdrop-filter: blur(10px);
        border-radius: 20px;
        padding: 2rem;
        margin: 2rem auto;
        max-width: 800px;
    }

    .edit-title {
        color: #fff;
        font-size: 2rem;
        text-align: center;
        margin-bottom: 2rem;
        font-weight: 500;
    }

    .form-label {
        color: rgba(255, 255, 255, 0.8);
        font-size: 0.9rem;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        margin-bottom: 0.5rem;
    }

    .form-control {
        background: rgba(255, 255, 255, 0.08);
        border: 1px solid rgba(255, 255, 255, 0.2);
        border-radius: 10px;
        color: rgba(255, 255, 255, 0.9);
    }

    .form-control::placeholder {
        color: rgba(255, 255, 255, 0.6);
    }

    .form-control:focus {
        background: rgba(255, 255, 255, 0.12);
        border-color: rgba(255, 255, 255, 0.4);
        color: rgba(255, 255, 255, 0.9);
        box-shadow: none;
    }

    .btn-group {
        display: flex;
        gap: 1rem;
        margin-top: 2rem;
    }

    .btn-save, .btn-cancel, .btn-home {
        flex: 1;
        padding: 0.8rem;
        border-radius: 25px;
        transition: all 0.3s ease;
        text-align: center;
        text-decoration: none;
    }

    .btn-save {
        background: rgba(255, 255, 255, 0.2);
        border: 1px solid rgba(255, 255, 255, 0.3);
        color: #fff;
    }

    .btn-cancel {
        background: rgba(255, 255, 255, 0.1);
        border: 1px solid rgba(255, 255, 255, 0.2);
        color: rgba(255, 255, 255, 0.8);
    }

    .btn-home {
        background: rgba(29, 0, 134, 0.6);
        border: 1px solid rgba(255, 255, 255, 0.2);
        color: rgba(255, 255, 255, 0.8);
    }

    .btn-save:hover, .btn-cancel:hover, .btn-home:hover {
        transform: translateY(-2px);
        color: #fff;
        background: rgba(255, 255, 255, 0.25);
    }
</style>

<div class="video-background">
    <iframe 
        src="https://youtube.com/embed/DiMnGq1zbNU?autoplay=1&mute=1&loop=1&controls=0&showinfo=0&rel=0&playlist=DiMnGq1zbNU&modestbranding=1" 
        allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
        frameborder="0" 
        allowfullscreen
    ></iframe>
</div>

<div class="container">
    <div class="edit-section">
        <h1 class="edit-title">Modifier mon profil</h1>
        
        {{ form_start(form) }}
            <div class="row mb-4">
                <div class="col-md-6">
                    {{ form_row(form.pseudo) }}
                </div>
                <div class="col-md-6">
                    {{ form_row(form.profilePicture) }}
                </div>
            </div>

            <div class="mb-4">
                {{ form_row(form.biography) }}
            </div>

            <div class="row mb-4">
                <div class="col-md-6">
                    {{ form_row(form.location) }}
                </div>
                <div class="col-md-6">
                    {{ form_row(form.birthDate) }}
                </div>
            </div>

            <div class="mb-4">
                {{ form_row(form.socialMediaLinks) }}
            </div>

            <div class="btn-group">
                <button type="submit" class="btn-save">Enregistrer</button>
                <a href="{{ path('app_profile') }}" class="btn-cancel">Annuler</a>
                <a href="{{ path('app_home') }}" class="btn-home">Accueil</a>
            </div>
        {{ form_end(form) }}
    </div>
</div>
{% endblock %} 